fastapi>=0.110
uvicorn>=0.27
pydantic>=2.6
anthropic>=0.40
reportlab>=4.0
python-docx>=1.1
# NOT optional: every provider except Anthropic (Groq, NVIDIA, Gemini, OpenAI,
# and any custom OpenAI-compatible endpoint) is called through this SDK's
# client - which is most deployments, since Groq/NVIDIA are the free-tier
# defaults. Only skip this if RESUME_PROVIDER=anthropic is hardcoded forever.
openai>=1.40
